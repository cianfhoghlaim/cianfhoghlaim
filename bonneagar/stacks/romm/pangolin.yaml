# =============================================================================
# romm — Pangolin private-resource route
# =============================================================================
# Generated by scripts/generate-stack-pangolin-yaml.ts on 2026-07-08T17:46:34.751Z
#
# Routes romm.cianfhoghlaim.ie → romm container at port 3000.
# TinyAuth Member role required (operator + admins only — see
# bonneagar/PANGOLIN-SETUP.md for the role hierarchy).
#
# If the port is wrong, edit .env.example (LOCKET_PORT) + this file
# to match the actual service port in compose.yaml.
# =============================================================================

pangolin:
  private-resources:
    romm:
      name: romm
      mode: http
      full-domain: romm.cianfhoghlaim.ie
      destination-port: 3000
      protocol: http
      roles[0]: tinyauth@file
