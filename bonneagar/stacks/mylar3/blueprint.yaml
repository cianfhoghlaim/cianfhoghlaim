# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
#
# Apply via the Pangolin REST API or `pangolin/apply-blueprint.sh`.
# Active site: bunchloch (this MacBook). Later we add arm1-oci as a
# second site for active-active OCI failover.

private-resources:
  mylar3:
    name: "Mylar3"
    mode: "http"
    destination-port: 8090
    full-domain: "mylar3.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    sites:
      - bunchloch
