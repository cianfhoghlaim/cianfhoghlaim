# Oideachais — Komodo Resource Sync Blueprint

name: cianfhoghlaim
description: "Celtic Education Lakehouse Engine — dagster webserver + FastAPI + TanStack Start frontend + Agno AgentOS + Google ADK agents"
type: stack
run_directory: bonneagar/stacks/cianfhoghlaim
files:
  - compose.yaml
  - sidecar.yaml
schedule:
  enabled: false
metadata:
  category: engineering
  ports: [3335, 8000, 3080, 7777, 7778]
  depends_on: [lakehouse, litellm, langfuse]
