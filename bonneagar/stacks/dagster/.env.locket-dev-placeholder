# Dagster dev secrets file (empty placeholder for the locket /run/secrets/locket/secrets.env path)
# The dev overlay's locket is a no-op alpine container, so this file
# doesn't need any actual secrets. The .env.dev file provides the real
# secrets.
