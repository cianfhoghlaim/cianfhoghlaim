# =============================================================================
# dagster — Pangolin private-resource route
# =============================================================================
# Generated by scripts/generate-stack-pangolin-yaml.ts on 2026-07-08T17:46:34.748Z
#
# Routes dagster.cianfhoghlaim.ie → dagster container at port 8080.
# TinyAuth Member role required (operator + admins only — see
# bonneagar/PANGOLIN-SETUP.md for the role hierarchy).
#
# If the port is wrong, edit .env.example (LOCKET_PORT) + this file
# to match the actual service port in compose.yaml.
# =============================================================================

pangolin:
  private-resources:
    dagster:
      name: dagster
      mode: http
      full-domain: dagster.cianfhoghlaim.ie
      destination-port: 8080
      protocol: http
      roles[0]: tinyauth@file
