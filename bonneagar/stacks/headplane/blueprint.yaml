# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  headplane:
    name: "Headplane"
    mode: "http"
    destination-port: 3000
    full-domain: "hp.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "headplane"

