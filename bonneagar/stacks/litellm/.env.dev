POSTGRES_USER=lakekeeper
# Per the lakehouse-multi-subject-multi-model-rollout change: this was a
# real, leaked Postgres password committed to git. Replaced with the
# same dev-fallback placeholder bonneagar/stacks/lakehouse/.env.example
# already establishes -- the real value comes from
# infisical://dev-baile/lakehouse/postgres_password via locket at actual
# deploy time (see bonneagar/stacks/lakehouse/secrets.env). Rotating the
# leaked value out-of-band in the real Postgres instance/Infisical vault
# is separate manual follow-up, not an in-repo change.
POSTGRES_PASSWORD=devpassword
LITELLM_MASTER_KEY=sk-6b8a650830ec862303c75b8596a97ac0dcb5afa55621025142e788aa24107db9
LITELLM_SALT_KEY=69032bea8a1ad60eecbd802770025387
LITELLM_LOG=INFO
MLFLOW_TRACKING_URI=http://mlflow:5000
LANCEDB_API_KEY=
