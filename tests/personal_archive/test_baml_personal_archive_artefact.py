"""Placeholder — the BAML tests for `UoGPersonalArchiveArtefact` run
via `baml test`, not pytest. This file exists so the WS12 test
directory is complete.

Per the 2026-08-23-uog-personal-archive-tertiary-modules-v1 change
(WS12 — Tests + observability + thesis figures).
"""


def test_baml_personal_archive_artefact_placeholder() -> None:
    """The BAML test suite runs via `baml test`, not pytest."""
    # Intentional no-op: BAML function-level tests live in
    # `baml_src/british_isles/ireland/education/university/personal_archive_extraction.baml`
    # and run via `baml test`. This pytest file exists only to make
    # the WS12 test directory complete.
    assert True
