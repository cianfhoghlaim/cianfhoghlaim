"""Placeholder — the BAML tests for `UoGAssignment` run via
`baml test`, not pytest. This file exists so the WS12 test
directory is complete.

Per the 2026-08-23-uog-personal-archive-tertiary-modules-v1 change
(WS12 — Tests + observability + thesis figures).
"""


def test_baml_assignment_placeholder() -> None:
    """The BAML test suite runs via `baml test`, not pytest."""
    assert True
