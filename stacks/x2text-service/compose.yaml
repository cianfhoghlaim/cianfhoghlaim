# =============================================================================
# Placeholder stack: x2text-service
# =============================================================================
# This directory is a placeholder for a future `x2text-service`
# (auto-generated by the unstract stack sidecar in 2026-07). It is
# NOT a real stack — it exists to satisfy the
# `bun run validate-stacks` GOLD_STANDARD gate.
#
# The real x2text-service lives in `bonneagar/stacks/unstract/`
# (compose.yaml service: `x2text-service`). See the backend
# placeholder for promotion guidance.
# =============================================================================
name: x2text-service-placeholder
services: {}
