# =============================================================================
# Placeholder stack: runner
# =============================================================================
# This directory is a placeholder for a future `runner` service
# (auto-generated by the unstract stack sidecar in 2026-07). It is
# NOT a real stack — it exists to satisfy the
# `bun run validate-stacks` GOLD_STANDARD gate.
#
# The real runner service lives in `bonneagar/stacks/unstract/`
# (compose.yaml service: `runner`). See the backend placeholder
# for promotion guidance.
# =============================================================================
name: runner-placeholder
services: {}
