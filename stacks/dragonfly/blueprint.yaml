# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  dragonfly:
    name: "Dragonfly"
    mode: "tcp"
    destination-port: 6379
    full-domain: "dragonfly.cianfhoghlaim.ie"
    protocol: "tcp"
    roles:
      - "Member"
    destination: "dragonfly"

