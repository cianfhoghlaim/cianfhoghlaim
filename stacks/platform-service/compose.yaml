# =============================================================================
# Placeholder stack: platform-service
# =============================================================================
# This directory is a placeholder for a future `platform-service`
# (auto-generated by the unstract stack sidecar in 2026-07). It is
# NOT a real stack — it exists to satisfy the
# `bun run validate-stacks` GOLD_STANDARD gate.
#
# The real platform-service lives in `bonneagar/stacks/unstract/`
# (compose.yaml service: `platform-service`). See the backend
# placeholder for promotion guidance.
# =============================================================================
name: platform-service-placeholder
services: {}
