# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  pipecat:
    name: "Pipecat (Voice AI)"
    mode: "http"
    destination-port: 8765
    full-domain: "pipecat.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "pipecat"

