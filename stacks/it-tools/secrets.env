# =============================================================================
# IT-TOOLS - Locket Secrets Template (dev-baile vault)
# =============================================================================
# COMMITTED: yes. PLAINTEXT: NEVER.
# Resolved at container runtime by the Locket sidecar.
#
# it-tools is a static front-end-only dev toolbox; it does not declare
# any runtime secret env vars. This file exists to satisfy the
# stack-doctor "must-have-secrets-env" gate + the LOCKET_MODE=watch header.
# To add a secret: append `KEY=infisical://dev-baile/it-tools/KEY` and
# register the value in the dev-baile Infisical project (path `/it-tools`).
# =============================================================================

LOCKET_MODE=watch
