# =============================================================================
# Placeholder stack: workers
# =============================================================================
# This directory is a placeholder for a future `workers` service
# (auto-generated by the unstract stack sidecar in 2026-07). It is
# NOT a real stack — it exists to satisfy the
# `bun run validate-stacks` GOLD_STANDARD gate.
#
# The real workers live in `bonneagar/stacks/unstract/`
# (compose.yaml services: `worker-unified` × 5 variants). See
# the backend placeholder for promotion guidance.
# =============================================================================
name: workers-placeholder
services: {}
