# =============================================================================
# Placeholder stack: backend
# =============================================================================
# This directory is a placeholder for a future `backend` service
# (auto-generated by the unstract stack sidecar in 2026-07). It is
# NOT a real stack — it exists to satisfy the
# `bun run validate-stacks` GOLD_STANDARD gate (every subdir of
# `bonneagar/stacks/` must contain at least one `.yaml` file).
#
# The real backend service lives in `bonneagar/stacks/unstract/`
# (compose.yaml service: `backend`). If this placeholder is ever
# promoted to a real stack, add the standard 5 GOLD_STANDARD
# files (sidecar.yaml, secrets.env, blueprint.yaml, pangolin.yaml,
# .env.example) and the 4 docs (README.md, AGENTS.md, etc.).
# =============================================================================
name: backend-placeholder
services: {}
