# Cross-repo sync plan: 2026-07-14-t1-docs-stacks-and-secrets-env-v1

This change touches **2 of the 3 repos**: cianfhoghlaim + bonneagar.
The leabharlann repo is unaffected.

## Repo 1: `bonneagar` (separate worktree at `./bonneagar/`)

**Branch**: `pick-5b-bonneagar-v5-continuation`
**Remote**: `bonneagar` → `https://github.com/cianfhoghlaim/bonneagar.git`

### Files edited (13)

1. `bonneagar/stacks/it-tools/secrets.env`
2. `bonneagar/stacks/komodo/secrets.env`
3. `bonneagar/stacks/llama-swap/secrets.env`
4. `bonneagar/stacks/marimo/secrets.env`
5. `bonneagar/stacks/searxng/secrets.env`
6. `bonneagar/stacks/actual/secrets.env`
7. `bonneagar/stacks/audiobookshelf/secrets.env`
8. `bonneagar/stacks/dozzle/secrets.env`
9. `bonneagar/stacks/enclosed/secrets.env`
10. `bonneagar/stacks/Kapowarr/secrets.env`
11. `bonneagar/stacks/LetterFeed/secrets.env`
12. `bonneagar/stacks/pastemax/secrets.env`
13. `bonneagar/stacks/pinchflat/secrets.env`

All 13 are `secrets.env` refactors — 5 plaintext → canonical
`infisical://dev-baile/<stack>/<key>`; 8 placeholder comments → canonical
`LOCKET_MODE=watch` + header pattern.

### Commit shape

```
fix(secrets-env): refactor 13 files to v4 infisical:// contract

Closes the secrets.env half of open issue #107 (the T1 follow-up
per `2026-07-14-t1-docs-stacks-and-secrets-env-v1`).

5 plaintext conversions (storing dev creds or placeholder config):
- it-tools: 1-line placeholder -> LOCKET_MODE=watch + canonical header
- komodo: plaintext dev creds -> infisical://dev-baile/komodo/{database_username,database_password}
- llama-swap: placeholder GATEWAY_API_KEY -> removed; LOCKET_MODE=watch added
- marimo: 1-line placeholder -> LOCKET_MODE=watch + canonical header
- searxng: plaintext SEARXNG_REDIS_URL -> infisical://dev-baile/searxng/redis_url

8 placeholder-comment conversions (auto-generated by scripts/fix-secrets-env-placeholders.ts in the cianfhoghlaim repo, then mirrored to this commit):
- actual, audiobookshelf, dozzle, enclosed, Kapowarr, LetterFeed,
  pastemax, pinchflat

After this commit:
- `bun run validate-stacks` (with DOCS_DIR=docs/stacks exported) reports 0 `secrets.env has no infisical:// refs` warnings
- The openspec change `2026-07-14-t1-docs-stacks-and-secrets-env-v1` can archive
```

### Push target

`git -C bonneagar push bonneagar pick-5b-bonneagar-v5-continuation`

## Repo 2: `cianfhoghlaim` (this repo)

**Branch**: `pick-4-biep-v1`
**Remote**: `origin` → `https://github.com/cianfhoghlaim/cianfhoghlaim.git`

### Files created (13)

1. `scripts/generate-stack-docs.ts` (Bun script; the 4th T1 generator)
2. `scripts/fix-secrets-env-placeholders.ts` (Bun script; idempotent)
3. `docs/stacks/drop.md`
4. `docs/stacks/hermes.md`
5. `docs/stacks/ludusavi.md`
6. `docs/stacks/moonlight.md`
7. `docs/stacks/newt.md`
8. `docs/stacks/olm-arm1-oci.md`
9. `docs/stacks/storybook.md`
10. `docs/stacks/sunshine.md`
11. `docs/stacks/wave2.md`
12. `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/proposal.md`
13. `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/tasks.md`

### Files created (under specs/) (3)

1. `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/specs/infrastructure-stacks/spec.md` (MODIFIED Requirement)
2. `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/specs/infrastructure-stacks-documentation/spec.md` (MODIFIED Requirement)
3. `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/cross-repo-sync.md` (this file)

### Files edited (0 — the cianfhoghlaim-side spec files are NOT modified; the delta lives in the change folder under `specs/`)

> The `infrastructure-stacks` and `infrastructure-stacks-documentation`
> specs in `openspec/specs/` are NOT modified by this change. The
> delta content lives entirely in
> `openspec/changes/2026-07-14-t1-docs-stacks-and-secrets-env-v1/specs/`
> per the openspec convention (changes are deltas; archives copy
> the deltas into the canonical specs). When this change archives,
> the 2 ADDED Requirements are merged into the canonical specs.

### Commit shape

```
chore(docs): ship 9 per-stack docs + 2 generators (closes issue #107 docs half)

Implements openspec change 2026-07-14-t1-docs-stacks-and-secrets-env-v1
(2 MODIFIED spec deltas on infrastructure-stacks +
infrastructure-stacks-documentation).

Phase 1: Per-stack docs (9 files)
- Creates scripts/generate-stack-docs.ts (Bun script that
  iterates over bonneagar/stacks/<name>/ and generates
  docs/stacks/<name>.md from compose.yaml + README.md +
  blueprint.yaml + pangolin.yaml). Supports --apply (write files),
  --stack=<name> (single-stack filter), and --dry-run (default).
  Handles compose.yaml AND docker-compose.yaml fallback (for newt),
  recognizes the wave2 multi-stack staging area, skips IP-prefixed
  port bindings like 127.0.0.1:9119:9119.
- Creates scripts/fix-secrets-env-placeholders.ts (idempotent Bun
  script that converts the 8 placeholder comments to the canonical
  v4 form).
- Generates 9 docs/stacks/<name>.md files (one per active stack
  that was missing one):
  - drop, hermes, ludusavi, moonlight, newt, olm-arm1-oci,
    storybook, sunshine, wave2
- Each doc has Purpose / Why-GitOps / Services / Image /
  Cross-references / Tags (the same 4-section template as the
  hand-written baseline in docs/stacks/cal-diy.md,
  docs/stacks/litellm.md, docs/stacks/infisical.md)

Phase 2: 2 MODIFIED spec deltas
- specs/infrastructure-stacks/spec.md: ADDED Requirement "All
  88 stacks have a `docs/stacks/<name>.md` cross-reference"
- specs/infrastructure-stacks-documentation/spec.md: MODIFIED
  Requirement "All secrets.env files use infisical://dev-baile/
  references" (strengthened to require the canonical form)
```

### Push target

`git push --set-upstream origin pick-4-biep-v1` (NOT `main`)

## Repo 3: `leabharlann` (separate worktree at `./leabharlann/`)

**No changes**. This change does not touch the leabharlann corpus.

## Order of operations

Per the convention established by
`2026-07-09-v6-drift-remediation-and-repo-boundary-lockdown-v1`, the
2 repos MUST be committed in this order:

1. **bonneagar first** — the 13 secrets.env refactors are the IaC
   tests' prerequisite; `bun run validate-stacks` reads these files
   to enforce the "must have infisical ref" gate
2. **cianfhoghlaim second** — the openspec change references the
   new bonneagar state in its acceptance criteria

After both commits, the openspec change can be archived.

## Acceptance gates (both repos)

- `openspec validate 2026-07-14-t1-docs-stacks-and-secrets-env-v1 --strict` passes (cianfhoghlaim)
- `DOCS_DIR=docs/stacks bun run validate-stacks` reports 0 `missing-doc` and 0 `secrets.env has no infisical:// refs` warnings (both repos)
- `ls -d bonneagar/stacks/*/ | wc -l` returns **89** (bonneagar)
- `ls docs/stacks/*.md | wc -l` returns **98** (cianfhoghlaim)
- `git status` clean in both worktrees
- Pushed to `origin/pick-4-biep-v1` (cianfhoghlaim) +
  `bonneagar/pick-5b-bonneagar-v5-continuation` (bonneagar)
