# Tasks: tuatha-capture-macos-native (v1)

- [ ] 1. SwiftPM project compiles.
- [ ] 2. `tuatha-capture doctor` passes on M-series macOS 15+.
- [ ] 3. `tuatha-capture daemon` boots and writes a manifest + keyframes.
- [ ] 4. LaunchAgent loads and survives a reload cycle.
- [ ] 5. The JSON-RPC `start_run`, `stop_run`, `mark_event`, `list-windows` methods work end-to-end.
