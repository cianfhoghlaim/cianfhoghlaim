# A2 — Stub (deferred to bonneagar repo)

The A2 change (`2026-08-01-bonneagar-iac-namespace-alignment-v1`) targets
the **bonneagar repo** (not the cianfhoghlaim repo), per the post-v7
flatten split. This stub change is left in the cianfhoghlaim repo
for tracking purposes only.

The A2 implementation lives in the separate `bonneagar` repo
(`git mv bonneagar/stacks/oideachais bonnegar/stacks/cianfhoghlaim` +
7 Komodo file renames + Cognee/lakehouse/agent-os/infisical updates).
The openspec change proposal + tasks are committed here for visibility.
The actual `git mv` commands need to be run from the bonneagar repo
clone.

See:
- `openspec/changes/2026-08-01-bonneagar-iac-namespace-alignment-v1/proposal.md`
- `openspec/changes/2026-08-01-bonneagar-iac-namespace-alignment-v1/tasks.md`
- `openspec/changes/2026-08-01-bonneagar-iac-namespace-alignment-v1/specs/infrastructure-stacks/spec.md`
