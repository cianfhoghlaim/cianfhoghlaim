# 2026-07-26-biep-v3-root-namespace-rename-v1 — Tasks

## Pre-implementation

- [ ] Verify openspec CLI ≥1.4: `openspec --version` → 1.4.1
- [ ] Verify the BIEP v2 stack + 2026-07-25 refactor batch merged
- [ ] Verify the ccc code index is fresh: `bun run ccc:index`

## Stage 1 — File renames

- [ ] `git mv dlt/common/destinations_oideachais.py dlt/common/destinations_cianfhoghlaim.py`
- [ ] `git mv web/apps/oideachais-web web/apps/cianfhoghlaim-web`
- [ ] Rename 10 openspec specs:
  - `oideachais-baml-schemas` → `cianfhoghlaim-baml-schemas`
  - `oideachais-cocoindex-v1-migration` → `cianfhoghlaim-cocoindex-v1-migration`
  - `oideachais-cognify-knowledge-graph` → `cianfhoghlaim-cognify-knowledge-graph`
  - `oideachais-email-triage` → `cianfhoghlaim-email-triage`
  - `oideachais-leabharlann` → `cianfhoghlaim-leabharlann`
  - `oideachais-marimo-dashboards` → `cianfhoghlaim-marimo-dashboards`
  - `oideachais-pdf-processing` → `cianfhoghlaim-pdf-processing`
  - `oideachais-pipeline` → `cianfhoghlaim-pipeline`
  - `oideachais-semantic-search` → `cianfhoghlaim-semantic-search`
  - `oideachais-university-deep-extraction` → `cianfhoghlaim-university-deep-extraction`

## Stage 2 — Content renames

- [ ] Run `find ... -exec sed -i '' -e 's|oideachais\.|cianfhoghlaim.|g' ... {} \;`
  across `baml_src/british_isles/`, `dlt/british_isles/`, `dlt/common/`,
  `cocoindex_flows/subjects/`, `cocoindex_flows/british_isles/`, `motherduck/`,
  `orchestration/`, `web/apps/cianfhoghlaim-web/`, `web/hono-api/`,
  `openspec/specs/`, `openspec/changes/`, `.agents/`, `notebooks/*.py`,
  `notebooks/_shared/`, `AGENTS.md`, `README.md`, `mise.toml`, `docs/`
  (5 patterns: `oideachais.`, `oideachais_`, `oideachais-`, `oideachais/`,
  quoted forms)
- [ ] Update `dlt/common/destinations_cianfhoghlaim.py:DEFAULT_NAMESPACE`
  from `"oideachais"` → `"cianfhoghlaim"`
- [ ] Update `mise.toml` aliases if any reference `oideachais`
- [ ] Update `web/apps/cianfhoghlaim-web/package.json` name field

## Stage 3 — Out-of-scope files (do NOT rename)

- [ ] Skip `_legacy/` directories (archived files preserve history)
- [ ] Skip `commonwealth/` directories (NZ, AU, IN, CA, ZA, NG)
- [ ] Skip `dlt/api_sources/` (generic DLT sources)
- [ ] Skip `baml_src/shared/baml_client_ts/` (regenerated by `baml-cli generate`)
- [ ] Skip `archive/` directories

## Stage 4 — Validation

- [ ] `grep -rn "oideachais" baml_src/british_isles/ dlt/british_isles/
   cocoindex_flows/subjects/ cocoindex_flows/british_isles/ dlt/common/ motherduck/
   orchestration/ web/apps/cianfhoghlaim-web/ web/hono-api/ openspec/specs/
   openspec/changes/ .agents/ AGENTS.md README.md mise.toml docs/`
  returns ZERO non-`_legacy`/non-`commonwealth`/non-`api_sources`/non-`archive` matches
- [ ] `bun run setup` runs cleanly
- [ ] `mise run lint:skills` (53/53)
- [ ] `cd baml_src && uv run baml-cli generate`
- [ ] `dg check yaml`
- [ ] `openspec list --specs` lists the 10 renamed specs (no `oideachais-` ones)

## Stage 5 — Spec delta + validation

- [ ] Write the spec delta to
  `openspec/changes/2026-07-26-biep-v3-root-namespace-rename-v1/specs/cross-region-pipeline/spec.md`
- [ ] Run `openspec validate 2026-07-26-biep-v3-root-namespace-rename-v1 --strict`
- [ ] Commit the change on a dedicated branch
- [ ] Open a PR on `origin/main` referencing this change
- [ ] After the PR merges and the change is deployed, run
  `openspec archive 2026-07-26-biep-v3-root-namespace-rename-v1 --yes`

## Post-implementation hand-off

- [ ] File any remaining bugs as GitHub issues
- [ ] Update `docs/cianfhoghlaim-namespace-migration.md` with the migration notes
- [ ] Run `./scripts/sync_agent_docs.sh` per the global agent protocol