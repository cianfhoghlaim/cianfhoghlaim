## Why

The web monorepo consolidation + agent integration
(`2026-08-13-web-monorepo-consolidation-and-agent-integration-v1`,
162 tasks) is the largest in-flight change. Bundling it as its own
topic batch gives it the visibility it deserves (a single change
hidden inside a batch of smaller ones is easy to lose track of).

## What Changes

- Cross-batch web monorepo topic contract: the 162 tasks ship as
  one milestone, not 162 separate tracks.
- Records the milestone's blast radius in `design.md`.

## Capabilities

(none added; pure bundling)

## Impact

- **Affected changes**: 1 bundled
- **Affected code**: none — this change is metadata-only