# Tasks: add-data-engineering-space-spec

## 1. Add the spec

- [x] Create `openspec/specs/data-engineering-space/spec.md` with:
      - 1 ADDED Requirement: "data-engineering Space must use the
        KCG canonical stack"

## 2. Validate + commit + push + archive

- [x] `openspec validate add-data-engineering-space-spec --strict`
- [x] Commit + archive + push
