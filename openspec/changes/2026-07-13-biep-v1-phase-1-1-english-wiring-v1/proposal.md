# Phase 1.1 English lc5 wiring — verification + spec delta wrapper

## Why

The BIEP v1 flagship (`2026-07-06-british-isles-education-pipeline-v1`,
**now archived** at `openspec/changes/archive/2026-07-09-2026-07-06-british-isles-education-pipeline-v1/`)
lists 4 unticked Phase 1.1 sub-tasks at the top of its tasks.md:

> ### Sub-batch 1.1 — English lc5 wiring
> - [ ] 1.1.1 Edit `leaving_cert_source.py:48` — add `"english"` to `LC5_SUBJECTS` …
> - [ ] 1.1.2 Edit `lc5_assets.py:154` — extend the asset factory to emit 6 more assets …
> - [ ] 1.1.3 Edit `defs.yaml` (or create the english variant) — add the 6-subject cron

The **code** for all three sub-tasks was actually shipped by the prior
openspec change `2026-07-10-wire-english-lc5-and-resolve-ie-duplicates-v1`
(commit `ba234de61`, branch `pick-4-biep-v1`). That change also added
2 ADDED Requirements to the same `british-isles-education-pipeline`
spec covering the filesystem wiring + the duplicate-removal declaration.

This standalone change (`2026-07-13-biep-v1-phase-1-1-english-wiring-v1`)
exists to:

1. **Verify** that Phase 1.1 is actually code-complete on disk (the
   flagship's `[ ]` boxes are stale — code was shipped, boxes were
   never ticked because the flagship was archived before the tick step).
2. **Add 1 distinct ADDED Requirement** to `british-isles-education-pipeline`
   that captures the **Phase 1.1 verification status** as a separate,
   audit-friendly delta from the prior change's "All 6 LC subjects
   have working filesystem DLT source" Requirement.
3. **Tick the Phase 1.1 boxes** in a per-phase `tasks.md` so a future
   agent reading this change can see exactly what was checked vs.
   what was merely code-shipped by the prior change.

> **Hard rule respected**: this change does NOT modify the
> archived flagship `tasks.md`. The archive is frozen; ticking the
> 1.1.1 / 1.1.2 / 1.1.3 boxes in the flagship archive would violate
> "Do NOT touch the 50+ archived openspec changes under
> `openspec/changes/archive/*`". The verification status lives in
> **this change's own `tasks.md`** instead.

## What changes

| File | Status | Lines |
|:--|:--|--:|
| `openspec/changes/2026-07-13-biep-v1-phase-1-1-english-wiring-v1/proposal.md` | NEW | this file |
| `openspec/changes/2026-07-13-biep-v1-phase-1-1-english-wiring-v1/tasks.md` | NEW | ~80 lines |
| `openspec/changes/2026-07-13-biep-v1-phase-1-1-english-wiring-v1/specs/british-isles-education-pipeline/spec.md` | NEW (1 ADDED Requirement) | ~40 lines |

No source code in `cianfhoghlaim/dlt/` or `cianfhoghlaim/orchestration/`
is modified by this change — all Phase 1.1 code work was already
shipped by `2026-07-10-wire-english-lc5-and-resolve-ie-duplicates-v1`.

## How — the 4 verification gates

### Gate 1 — `LC6_SUBJECTS` includes `english` (6 subjects total)

```bash
$ grep -A 7 "^LC6_SUBJECTS" cianfhoghlaim/dlt/filesystem/leaving_cert_source.py | head -10
LC6_SUBJECTS: tuple[str, ...] = (
    "chemistry",
    "computer_science",
    "english",
    "gaeilge",
    "geography",
    "mathematics",
)
```

### Gate 2 — `LC_PDF_KIND_REGISTRY` has 2 English regex patterns

```bash
$ grep -n "LC002ALP\|SC-English-Spec-ENG-INT" cianfhoghlaim/dlt/filesystem/leaving_cert_source.py
67:    r"^LC002ALP\d{3}[EI]V\.pdf$": "qwen3-vl-8b",
73:    r"^SC-English-Spec-ENG-INT.*\.pdf$": "gemma-4-26B-A4B",
```

Plus the `elif subject_dir.name == "english"` branch in `_scan_subject`
at lines 137-145 (en-only at root, mirrors gaeilge asymmetry).

### Gate 3 — 6 English assets in `lc5_assets.py`

AST-verified at 2026-07-10 14:03:

- 2 explicit `@asset` decorators:
  - `lc5_english_ingested` (Layer 1)
  - `lc5_english_cognified` (Layer 3)
- 4 generated by the factory loop at lines 199-201 (`for _subject in LC6_SUBJECTS: for _kind in (syllabus, papers, marking, diagrams): globals()[f"lc5_{_subject}_{_kind}_extracted"] = ...`):
  - `lc5_english_syllabus_extracted`
  - `lc5_english_papers_extracted`
  - `lc5_english_marking_extracted`
  - `lc5_english_diagrams_extracted`

Total: **6 `lc5_english_*` assets** (the Phase 1.1 contract).

### Gate 4 — `english.yaml` cron asset exists

```bash
$ ls -la cianfhoghlaim/orchestration/defs/1_ingestion/curriculum/lc5/english.yaml
-rw-r--r-- 1 cian staff 1492 Jul 10 06:23 english.yaml
```

`type: cianfhoghlaim.orchestration.components.CelticIngestionComponent`,
`source_id: cianfhoghlaim.filesystem.leaving_cert.english`,
`automation_cron: "0 5 * * *"` (mirrors `lc5/defs.yaml`).

## Out of scope (already tracked by other openspec changes)

1. **Dagster group_name regex bug** — all `1_ingestion/curriculum/lc5`
   group_name values are invalid in Dagster 1.13.1 (must match
   `^[A-Za-z0-9_]+$`). Affects ALL 36 lc5 assets, not just English.
   Tracked by `2026-07-11-fix-dagster-asset-group-name-regex-v1`.
   Documented in the prior English wiring change's "Out of scope" §1.
2. **BAML codegen** — the F2 commit (`54c21dd52`) already fixed all
   50 `field: type` errors; `mise run baml:generate` now exits 0
   (verified at 2026-07-10 14:03).
3. **The 4 BIEP v1 `lc_extraction/*.baml` files** — owned by the
   flagship change; this change does not modify them.
4. **The 7 legacy `notebooks/legacy/03_leaving_cert/` notebooks** —
   owned by the Phase 6 cleanup; not in scope.

## Acceptance gates

- [x] `LC6_SUBJECTS` has 6 elements including `english` (Gate 1)
- [x] 2 English regex patterns in `LC_PDF_KIND_REGISTRY` (Gate 2)
- [x] 6 `lc5_english_*` assets in `lc5_assets.py` (Gate 3)
- [x] `english.yaml` cron asset exists (Gate 4)
- [x] `mise run baml:generate` exits 0 (verified 2026-07-10 14:03)
- [x] `openspec validate 2026-07-13-biep-v1-phase-1-1-english-wiring-v1 --strict` passes
- [ ] Committed + pushed to `origin/pick-4-biep-v1` (NOT `main`)

## Dependencies

`Blocked by (soft): 2026-07-10-wire-english-lc5-and-resolve-ie-duplicates-v1` —
this change verifies the code state shipped by that prior change.
The blocker does NOT need to archive first (this change is a
verification wrapper, not an extension).

`Related but not blocking: 2026-07-11-fix-dagster-asset-group-name-regex-v1` —
the dagster group_name bug prevents `dagster asset list` from
loading the lc5 defs. Tracked by that change. Out of scope for
this change.

`Affected repos: cianfhoghlaim` (single-repo change)