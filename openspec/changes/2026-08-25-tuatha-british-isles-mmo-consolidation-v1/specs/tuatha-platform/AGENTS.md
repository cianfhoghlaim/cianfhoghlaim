# `tuatha-platform` — Agent Routing

> `tuatha-platform` is the DEPRECATED capability (superseded by
> `cianfhoghlaim-educational-mmo` per the
> `2026-08-25-tuatha-british-isles-mmo-consolidation-v1`
> change). The 1-release back-compat window is the active
> period; this spec is not eligible for any new ADDED Requirements.

## Routing

Load this AGENTS.md when the parent spec (`./spec.md`) is in scope
during the 1-release back-compat window.

After the back-compat window closes (per the `tuatha-british-isles-mmo`
delta in the consolidation change), this spec moves to
`openspec/specs/_archive/tuatha-platform/` and this AGENTS.md is
deleted.

## Quick start

```bash
# Verify the spec is still in scope (1-release back-compat)
openspec list --specs | grep tuatha-platform
# (if it shows up, it's still in scope; if not, it's archived)
```

## Key sources

- `openspec/specs/tuatha-platform/spec.md` — the deprecated spec
- `openspec/specs/cianfhoghlaim-educational-mmo/spec.md` — the
  spec that superseded this one
- `openspec/specs/tuatha-british-isles-mmo/spec.md` — the new
  canonical Tuatha project capability

## Adjacent specs

- `openspec/specs/cianfhoghlaim-educational-mmo/spec.md` — the
  spec that superseded this one
- `openspec/specs/tuatha-british-isles-mmo/spec.md` — the new
  canonical Tuatha project capability

## DO NOT

- **Never** add NEW capability to this spec — the 4 original
  Requirements (the Pent-Elemental Cosmology + the Babylon.js 3D
  game front-end + the SpacetimeDB v2 game engine backend + the
  Crypteolas financial token) are superseded by
  `cianfhoghlaim-educational-mmo` + `tuatha-british-isles-mmo`
- **Never** add NEW files to `openspec/changes/<change-id>/specs/tuatha-platform/`
  for any new change — the 1-release back-compat window is the
  only period during which this delta folder is editable
- **Never** reference the Babylon.js / SpacetimeDB / Crypteolas /
  Pent-Elemental Cosmology / Anam Cara / Brown Ajah theming from
  any new code or spec

## Skill pointers

- [`centralized-registry`](../.agents/skills/centralized-registry/SKILL.md) —
  the canonical model registry (for the legacy 5 Pent-Elemental
  models + the deprecated Babylon.js / SpacetimeDB / Crypteolas
  references that are archived)
- [`british-isles-formative-assessment`](../.agents/skills/british-isles-formative-assessment/SKILL.md) —
  the canonical 5 curriculum frameworks + the 4 feedback channels
  that superseded the legacy Anam Cara + Brown Ajah theming

<!-- generated: 2026-08-25 by 2026-08-25-tuatha-british-isles-mmo-consolidation-v1; do not hand-edit -->
