# Tasks — cianchosaint Sister Umbrella Mirror v1

- [ ] **1** Adopt the 5 cianfhoghlaim-side umbrellas as
  dependencies.
- [ ] **2** Mirror the 10 cianchosaint-side changes to the
  cianfhoghlaim-side mirror directories.
  **Verification:** `git ls-files openspec/changes/2026-MM-DD-cianchosaint-*-mirror-v1/`
  reports 10 mirror directories.
- [ ] **3** Wire the per-PR reciprocal mirror CI gate.
- [ ] **4** Wire the per-quadrant DuckLake `metadata_schema` to
  `croilar`.
- [ ] **5** Wire the per-sister Langfuse project (`cianchosaint-dev` +
  `cianchosaint-prod`).

**Total tasks:** 5.
