## ADDED Requirements

### Requirement: Familiar Dynamic NFT Tables

The system SHALL extend the cianfhoghlaim-educational-mmo spec with
3 new Convex tables: `familiars.ts`, `anam_particles.ts`,
`familiar_evolution_log.ts`.

#### Scenario: Familiar table materialises
- **WHEN** the user invokes `mise run convex:dev`
- **THEN** all 3 Convex tables SHALL deploy