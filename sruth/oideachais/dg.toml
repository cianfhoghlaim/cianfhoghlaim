# oideachais — Celtic education data platform (228 Dagster assets)
# Phase 0.1 of lateralise-british-isles-domains + refactor-quadrants-to-sruth.
# Registers the oideachais Dagster code-location with auto-discovery
# from sruth/oideachais/data_platform/dagster_defs/.

directory_type = "project"

[project]
name = "oideachais"

[project.python_environment]
uv_managed = true

[project.code_location]
module_name = "oideachais.data_platform.dagster_defs.definitions"
location_name = "oideachais"
