# meaisínfhoghlaim — AI/ML quadrant (12 agents, OCR/HTR, Celtic-language DLT)
# Phase 0.1 of lateralise-british-isles-domains. Registers a
# heartbeat-only Dagster code-location so the AI/ML quadrant shows
# up in `dg dev` from the repo root. Real DLT/Dagster wrappers around
# the 3 ML pipelines (dialect_classifier, irish_document_scanner,
# transcript_aligner) ship in a follow-on change.

directory_type = "project"

[project]
name = "meaisinfhoghlaim"

[project.python_environment]
uv_managed = true

[project.code_location]
module_name = "dagster_defs"
location_name = "meaisinfhoghlaim"
