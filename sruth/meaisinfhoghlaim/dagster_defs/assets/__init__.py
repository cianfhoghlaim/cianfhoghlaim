"""meaisínfhoghlaim.dagster_defs.assets — Dagster assets for the AI/ML quadrant.

Phase 0.2 of lateralise-british-isles-domains. Currently 4 heartbeat
assets that materialise to a no-op `MaterializeResult("ok")` so the
AI/ML quadrant is observable in `dg dev` from the repo root.
"""
