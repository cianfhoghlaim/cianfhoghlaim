gradio>=4.44.0
# Per the v4 trim (2026-06-29), the HF Space uses transformers + ZeroGPU
# (no litellm dependency — ZeroGPU is CPU + on-demand CUDA via @spaces.GPU)
# The 2 models used are:
#   - unsloth/gemma-3-4b-it-GGUF (4 GB) — suggestion model
#   - unsloth/gemma-4-26B-A4B-it-GGUF (14 GB MoE) — explanation model
# Both are loaded fresh on each @spaces.GPU call (ZeroGPU has no module-scope warmup)
spaces>=0.27.0
transformers>=4.46.0
torch>=2.5.0
huggingface-hub>=0.26.0
accelerate>=1.0.0
bitsandbytes>=0.43.0
