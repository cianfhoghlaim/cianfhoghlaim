# defs/1_ingestion/commonwealth/can/medicine/defs.yaml
#
# L1 ingestion for the Canada medicine DLT source.
#
# Per the canonical cross-region path contract this source lives at
# ``dlt/commonwealth/can/medicine/health_canada.py`` with
# ``source_id="commonwealth.can.medicine.health_canada"`` and lands in
# the DuckLake table ``oideachais.medicine.commonwealth.can``.
#
# Reference: openspec/changes/2026-07-11-commonwealth-pipeline-v1/
type: cianfhoghlaim.orchestration.components.CelticIngestionComponent
attributes:
  source_name: commonwealth_can_medicine
  module: cianfhoghlaim.dlt.commonwealth.can.medicine.health_canada
  schedule:
    cron: "0 4 * * *"
    timezone: UTC
  partitions:
    - region:
        values: ["commonwealth"]
    - country:
        values: ["can"]
    - language:
        values: ["en", "fr"]
  use_local_scrapes: true
  asset_check: commonwealth_can_medicine_partition_min
  asset_check_min_rows: 1
  tags:
    - commonwealth
    - can
    - medicine
    - ingestion
  metadata:
    openspec_change: 2026-07-11-commonwealth-pipeline-v1
    spec: commonwealth-pipeline
