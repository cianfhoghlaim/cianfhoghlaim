# defs/1_ingestion/commonwealth/aus/law/defs.yaml
#
# L1 ingestion for the Australia law DLT source.
#
# Per the canonical cross-region path contract this source lives at
# ``dlt/commonwealth/aus/law/federal_register_legislation.py`` with
# ``source_id="commonwealth.aus.law.federal_register_legislation"`` and lands in
# the DuckLake table ``oideachais.law.commonwealth.aus``.
#
# Reference: openspec/changes/2026-07-11-commonwealth-pipeline-v1/
type: cianfhoghlaim.orchestration.components.CelticIngestionComponent
attributes:
  source_name: commonwealth_aus_law
  module: cianfhoghlaim.dlt.commonwealth.aus.law.federal_register_legislation
  schedule:
    cron: "0 4 * * *"
    timezone: UTC
  partitions:
    - region:
        values: ["commonwealth"]
    - country:
        values: ["aus"]
    - language:
        values: ["en"]
  use_local_scrapes: true
  asset_check: commonwealth_aus_law_partition_min
  asset_check_min_rows: 1
  tags:
    - commonwealth
    - aus
    - law
    - ingestion
  metadata:
    openspec_change: 2026-07-11-commonwealth-pipeline-v1
    spec: commonwealth-pipeline
