# defs/3_model_lifecycle/cognify/cross_stage_cognify/defs.yaml
#
# L3 cognify placeholders. The 5-stage cross-stage cognify + the 3
# leabharlann cognify + the 3 cross-archive FalkorDB edge rules are
# emitted by the L2 materials Layer (BAML extraction) and the L4
# marimo dashboards (cross-stage cognify report). The L3 placeholder
# exists for lineage visibility.
type: dagster.AssetSpec
attributes:
  key: "3_model_lifecycle/cognify/cross_stage_cognify"
  group_name: 3_model_lifecycle/cognify
  description: "L3 placeholder: the 5-stage cross-stage cognify is implemented in the L2 + L4 layers; this placeholder preserves the lineage witness."
  automation_condition:
    type: eager
