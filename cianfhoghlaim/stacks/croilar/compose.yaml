# Thin re-export shim for the croilar deploy quartet (round 7).
# Canonical location: infrastructure/stacks/croilar/{compose,compose.dev,sidecar,secrets,Dockerfile.dagster}
---
# This file is now a 1-line re-export. The canonical deploy quartet is at
# infrastructure/stacks/croilar/. See
# openspec/changes/archive/2026-06-24-infrastructure-stack-doctor-v1/.
