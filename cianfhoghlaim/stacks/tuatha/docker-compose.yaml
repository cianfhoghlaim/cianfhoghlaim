# Thin re-export shim for the tuatha deploy quartet (round 7).
# Canonical location: infrastructure/stacks/tuatha/{pangolin,compose,compose.dev}.yaml
---
# See pangolin.yaml in this directory for the re-export note.
