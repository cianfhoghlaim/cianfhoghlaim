# browser/crawl4ai — the new Crawl4AI App Component.
#
# Implements the 4th per-domain browser Component
# (Crawl4AI self-hosted at port 11235).
# Wraps the cianfhoghlaim.core.browser.backends.selfhosted.crawl4ai_backend.Crawl4AIBackend
# into a `dg.asset` that updates the Crawl4AI App on every materialization.
#
# Per openspec/specs/agentic-frontend-frameworks/spec.md (browser capability).
---
type: cianfhoghlaim.assets._oideachais_dagster_defs.components.CelticDltSourceComponent
attributes:
  source_id: browser.crawl4ai
  asset_name: browser_crawl4ai_update
  group_name: browser
