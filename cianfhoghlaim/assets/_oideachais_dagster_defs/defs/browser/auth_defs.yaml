# browser/auth — the Skyvern+Stagehand opt-in Component.
#
# Implements the 6th per-domain browser Component.
# Skyvern and Stagehand are OFF by default (per Phase D.2/D.3).
# Set BROWSER_ENABLE_SKYVERN=1 or BROWSER_ENABLE_STAGEHAND=1
# env vars to enable.
#
# When disabled (default): this defs is a no-op stub.
# When enabled: registers the Skyvern/Stagehand DLT sources.
#
# Per openspec/specs/agentic-frontend-frameworks/spec.md (browser capability).
---
# Default: no-op (the Skyvern/Stagehand backends are off by default).
# When BROWSER_ENABLE_SKYVERN=1 or BROWSER_ENABLE_STAGEHAND=1 is set,
# the Python module conditionally imports + registers the DLT sources
# (see cianfhoghlaim/core/browser/__init__.py).
type: dg.python_module
attributes: {}
python_module: cianfhoghlaim.assets._oideachais_dagster_defs.defs.browser.auth_assets

