# browser/firecrawl — the Firecrawl fallback Component.
#
# Implements the 5th per-domain browser Component
# (Firecrawl paid fallback; uses the MCP API).
# Wraps the cianfhoghlaim.core.browser.backends.paid.firecrawl.FirecrawlBackend
# into a `dg.asset` that materialises the Firecrawl-monitored
# pages + the credit-budget summary.
#
# Per openspec/specs/agentic-frontend-frameworks/spec.md (browser capability).
---
type: cianfhoghlaim.assets._oideachais_dagster_defs.components.CelticDltSourceComponent
attributes:
  source_id: browser.firecrawl
  asset_name: browser_firecrawl_update
  group_name: browser
