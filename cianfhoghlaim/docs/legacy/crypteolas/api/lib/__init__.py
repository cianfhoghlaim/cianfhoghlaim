"""Stub for the legacy `api.lib` subpackage — see tuatha/crypteolas/STATUS.md.

The original `crypteolas_formative_assessment/` carried a `api/lib/`
subpackage that was never committed. The new structure keeps the
import paths stable by re-exporting the same names from a small set
of stub modules. Replace with real implementations as the
crypteolas FastAPI surface is rebuilt.
"""

from __future__ import annotations
