# =============================================================================
# KOMODO PERIPHERY - Blueprint
# =============================================================================
# Deployment configuration for Komodo resource sync

name: komodo-periphery
description: Komodo Periphery agent for server management

deployment:
  servers:
    - arm1-oci
    - arm1-oci
    - bunchloch
  priority: 10  # Deploy before application stacks

resources:
  cpu: 1 core
  memory: 1 GB

dependencies: []
