# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  vaultwarden:
    name: "Vaultwarden"
    mode: "http"
    destination-port: 8080
    full-domain: "vault.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "vaultwarden"

