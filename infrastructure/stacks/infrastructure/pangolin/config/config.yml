# Pangolin Enterprise Edition Configuration
# Docs: https://docs.pangolin.net/self-host/advanced/config-file
# PostgreSQL configured via POSTGRES_CONNECTION_STRING env var (injected by Locket).
# SERVER_SECRET also injected by Locket — takes priority over the placeholder below.

gerbil:
    start_port: 51820
    base_endpoint: "pangolin.cianfhoghlaim.ie"

app:
    dashboard_url: "https://pangolin.cianfhoghlaim.ie"
    log_level: "info"
    telemetry:
        anonymous_usage: false

domains:
    cianfhoghlaim:
        base_domain: "cianfhoghlaim.ie"
        cert_resolver: "letsencrypt"
        prefer_wildcard_cert: true

server:
    secret: "${SERVER_SECRET:-changeme}"
    trust_proxy: 1
    cors:
        origins: ["https://pangolin.cianfhoghlaim.ie"]
        methods: ["GET", "POST", "PUT", "DELETE", "PATCH"]
        allowed_headers: ["X-CSRF-Token", "Content-Type"]
        credentials: false

traefik:
    http_entrypoint: "web"
    https_entrypoint: "websecure"

flags:
    require_email_verification: false
    disable_signup_without_invite: true
    disable_user_create_org: false
    allow_raw_resources: true
    enable_integration_api: true
    disable_local_sites: false
