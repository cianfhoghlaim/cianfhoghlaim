# =============================================================================
# NIMTABLE - Pangolin Blueprint
# =============================================================================

private-resources:
  nimtable:
    name: "Nimtable"
    full-domain: "nimtable.cianfhoghlaim.ie"
    mode: http
    destination-port: 3000
    protocol: http
    sites:
      - arm1-oci
    roles:
      - Member
