# =============================================================================
# LAKEKEEPER - Pangolin Blueprint
# =============================================================================

private-resources:
  lakekeeper:
    name: "Lakekeeper"
    full-domain: "lakekeeper.cianfhoghlaim.ie"
    mode: http
    destination-port: 8181
    protocol: http
    sites:
      - bunchloch
    roles:
      - Member
