# Oideachais — Komodo Resource Sync Blueprint

name: oideachais
description: "Celtic Education Lakehouse Engine — dagster webserver + FastAPI + TanStack Start frontend"
type: stack
run_directory: infrastructure/stacks/engineering/oideachais
files:
  - compose.yaml
  - sidecar.yaml
schedule:
  enabled: false
metadata:
  category: engineering
  ports: [3335, 8000, 3000]
  depends_on: [lakehouse, litellm]
