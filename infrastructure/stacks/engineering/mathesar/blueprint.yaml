# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  mathesar:
    name: "Mathesar"
    mode: "http"
    destination-port: 8000
    full-domain: "mathesar.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "mathesar"

