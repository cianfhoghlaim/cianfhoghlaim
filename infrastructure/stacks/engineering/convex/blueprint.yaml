# =============================================================================
# CONVEX - Pangolin Blueprint
# =============================================================================
# Note: All resources are private (zero-trust via Pangolin).
# =============================================================================

private-resources:
  convex-backend:
    name: "Convex Backend API"
    full-domain: "convex-api.cianfhoghlaim.ie"
    mode: http
    destination: convex-backend:3210
    protocol: http
    sites:
      - bunchloch
    roles:
      - Member

  convex-site:
    name: "Convex Site"
    full-domain: "convex-site.cianfhoghlaim.ie"
    mode: http
    destination: convex-site:3211
    protocol: http
    sites:
      - bunchloch
    roles:
      - Member

  convex-dashboard:
    name: "Convex Dashboard"
    full-domain: "convex.cianfhoghlaim.ie"
    mode: http
    destination: convex-dashboard:6791
    protocol: http
    sites:
      - bunchloch
    roles:
      - Member
