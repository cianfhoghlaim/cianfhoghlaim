# =============================================================================
# MLFLOW - Pangolin Blueprint
# =============================================================================
# Defines private routing for MLflow via Pangolin/Newt.
# =============================================================================

private-resources:
  mlflow:
    name: "MLflow"
    full-domain: "mlflow.cianfhoghlaim.ie"
    mode: http
    destination-port: 5000
    protocol: http
    sites:
      - arm1-oci
    roles:
      - Member
