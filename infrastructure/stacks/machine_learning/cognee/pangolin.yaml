# Cognee topology — Pangolin private resource for the Cognee REST API.
# Reachable as cognee.cianfhoghlaim.ie (on the OCI control plane,
# proxied through the Pangolin WireGuard mesh).
pangea:
  private_resources:
    - name: cognee
      label: Cianfhoghlaim Cognee KG
      role: member
      host: bunchloch
      port: 8000
      scheme: http
      healthcheck: /health
      description: "Cianfhoghlaim Oideachais multi-stage knowledge graph (Cognee REST API)"
