# Cognee stack secrets env (template — actual values from Infisical via Locket)
# These are fallback default values for local dev; production is served by Locket.
COGNEE_POSTGRES_PASSWORD=devpassword
LITELLM_API_KEY=sk-1234
LANCEDB_API_KEY=devtoken
