# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  dagster:
    name: "Dagster Unified"
    mode: "http"
    destination-port: 3000
    full-domain: "dagster.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "dagster"

  dagster-daemon:
    name: "Dagster Daemon"
    mode: "http"
    destination-port: 3000
    full-domain: "dagster-daemon.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "dagster-daemon"

