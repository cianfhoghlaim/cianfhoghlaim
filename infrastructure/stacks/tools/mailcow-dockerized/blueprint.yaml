# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  mailcow-dockerized:
    name: "Mailcow-dockerized"
    mode: "http"
    destination-port: 8080
    full-domain: "mailcow-dockerized.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "mailcow-dockerized"

