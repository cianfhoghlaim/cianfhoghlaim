# Pangolin private-resource blueprint (generated from pangolin.yaml label form)
# See: https://docs.pangolin.net/manage/blueprints
private-resources:
  audiobookshelf:
    name: "Audiobookshelf"
    mode: "http"
    destination-port: 8080
    full-domain: "audiobookshelf.cianfhoghlaim.ie"
    protocol: "http"
    roles:
      - "Member"
    destination: "audiobookshelf"

