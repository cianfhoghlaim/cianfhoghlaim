[package]
name = "crypteolas-sync"
description = "SpacetimeDB module for Crypteolas DeFi analytics synchronization"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { workspace = true }
cianfhoghlaim-shared-types = { path = "../shared-types", features = ["spacetimedb"] }

[dev-dependencies]
# SpacetimeDB test utilities
