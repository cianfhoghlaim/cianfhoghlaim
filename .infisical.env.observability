# =============================================================================
# OBSERVABILITY - Langfuse + MLflow + Logfire
# =============================================================================
# Per the 2026-08-24-lakehouse-stack-doctor-and-env-var-cleanup-v1 change.
# See `.infisical.env.lakehouse` for the lakehouse-specific secrets.
#
# PR #4 (observability integration) will wire these into the otel-collector
# service + langfuse/mlflow/logfire sidecars.
# =============================================================================

# -----------------------------------------------------------------------------
# Logfire (Pydantic logfire — OpenTelemetry tracing)
# -----------------------------------------------------------------------------
# Stack: bonneagar/storage/logfire
# Dashboard: https://logfire.pydantic.dev
LOGFIRE_TOKEN=infisical://dev-baile/pydantic-logfire/write_token

# -----------------------------------------------------------------------------
# Langfuse (LLM observability — used by Cognee + Graphiti LLM calls)
# -----------------------------------------------------------------------------
# Stack: bonneagar/stacks/langfuse
LANGFUSE_PUBLIC_KEY=infisical://dev-baile/langfuse/public_key
LANGFUSE_SECRET_KEY=infisical://dev-baile/langfuse/secret_key
# Langfuse server URL (resolved from langfuse stack secrets.env)
# LANGFUSE_HOST=infisical://dev-baile/langfuse/host

# -----------------------------------------------------------------------------
# MLflow (Experiment tracking + model registry — used by Dagster assets)
# -----------------------------------------------------------------------------
# Stack: bonneagar/stacks/mlflow
# MLFLOW_TRACKING_URI resolved via Locket from dev-baile/mlflow/tracking_uri
# MLFLOW_ARTIFACT_ROOT uses Garage S3 (already configured via lakehouse garage-init)
