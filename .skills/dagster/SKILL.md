# Dagster Skill

## Context
Orchestrator for the data platform.

## Rules
- Start local environment with `uv run dagster dev -m data_platform.dagster_defs.definitions` inside the `oideachais` directory.
- `ireland/curriculum/` assets are MultiPartitioned by `language` and `subject` (e.g., `"en|mathematics"`).
