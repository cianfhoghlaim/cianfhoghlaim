/**
 * Per-subject Convex types for LC Applied Mathematics.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Applied Mathematics* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { IdField, SystemFields } from "convex/server";

import type {
  Applied MathematicsSyllabus,
  Applied MathematicsPaper,
  Applied MathematicsMarkingScheme,
  Applied MathematicsTopics,
} from "../../../../../baml_client/typescript/baml_client/lc/mathematics/client";

/**
 * The BAML-generated Applied MathematicsPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type Applied MathematicsPayload =
  | Applied MathematicsSyllabus
  | Applied MathematicsPaper
  | Applied MathematicsMarkingScheme
  | Applied MathematicsTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface Applied MathematicsDoc extends IdField<"applied_mathematics">, SystemFields {
  app: "oideachais";
  stage: "lc";
  subject: "applied_mathematics";
  language: "en" | "ga";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: Applied MathematicsPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML applied_mathematicsType alias for the schema file
export type applied_mathematicsType = Applied MathematicsPayload;
