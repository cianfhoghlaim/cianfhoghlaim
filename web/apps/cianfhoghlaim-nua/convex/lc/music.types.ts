/**
 * Per-subject Convex types for LC Music.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Music* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { IdField, SystemFields } from "convex/server";

import type {
  MusicSyllabus,
  MusicPaper,
  MusicMarkingScheme,
  MusicTopics,
} from "../../../../../baml_client/typescript/baml_client/lc/music/client";

/**
 * The BAML-generated MusicPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type MusicPayload =
  | MusicSyllabus
  | MusicPaper
  | MusicMarkingScheme
  | MusicTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface MusicDoc extends IdField<"music">, SystemFields {
  app: "oideachais";
  stage: "lc";
  subject: "music";
  language: "en" | "ga";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: MusicPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML musicType alias for the schema file
export type musicType = MusicPayload;
