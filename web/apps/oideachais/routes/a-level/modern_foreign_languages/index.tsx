/**
 * A-LEVEL Modern Foreign Languages — subject landing page.
 *
 * AUTO-GENERATED by scripts/schema-codegen/per-subject-routes.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Per openspec/changes/2026-08-13-web-monorepo-consolidation-and-agent-integration-v1/
 * specs/per-subject-coverage/spec.md.
 */

import { createFileRoute, Link } from "@tanstack/react-router";
import { CopilotKit } from "@copilotkit/react-core/v2";
import { useState } from "react";

import { useModernForeignLanguagesActions } from "@/lib/copilotkit/a-level/modern_foreign_languages";
import { useModernForeignLanguagesEvents } from "@/lib/ag-ui/a-level/modern_foreign_languages";

export const Route = createFileRoute("/a-level/modern_foreign_languages/")({
  component: ModernForeignLanguagesPage,
});

function ModernForeignLanguagesPage() {
  return (
    <CopilotKit runtimeUrl="/api/copilotkit/a-level/modern_foreign_languages">
      <div className="max-w-4xl mx-auto flex flex-col gap-8 h-full p-8">
        <div className="flex flex-col gap-2">
          <h1 className="text-4xl font-bold">
            Modern Foreign Languages (A-LEVEL)
          </h1>
          <p className="text-slate-600 text-lg">
            Modern Foreign Languages — AL-MFL — languages: en
          </p>
        </div>

        <SubjectActionsSection subject="modern_foreign_languages" stage="a-level" />

        <Link
          to="/a-level/modern_foreign_languages/$topicId"
          params={{ topicId: "AL-MFL-001" }}
          className="btn-tactile"
        >
          Open topic detail →
        </Link>
      </div>
    </CopilotKit>
  );
}

function SubjectActionsSection({
  subject,
  stage,
}: {
  subject: string;
  stage: string;
}) {
  useModernForeignLanguagesActions();

  return (
    <div className="grid grid-cols-2 gap-4">
      <ActionCard
        title="Get syllabus topics"
        description="Fetch the Modern Foreign Languages syllabus topics + learning outcomes"
        actionName="get_syllabus_topics"
      />
      <ActionCard
        title="Get exam papers"
        description="List exam papers by year + level"
        actionName="get_exam_papers"
      />
      <ActionCard
        title="Get marking schemes"
        description="Fetch marking schemes + rubrics"
        actionName="get_marking_schemes"
      />
      <ActionCard
        title="Semantic search"
        description="BGE-M3 vector search over the Modern Foreign Languages embeddings"
        actionName="semantic_search"
      />
    </div>
  );
}

function ActionCard({
  title,
  description,
  actionName,
}: {
  title: string;
  description: string;
  actionName: string;
}) {
  return (
    <button
      className="rounded-lg border p-4 text-left hover:bg-slate-50"
      data-action={actionName}
    >
      <h3 className="font-semibold">{title}</h3>
      <p className="text-sm text-slate-600">{description}</p>
    </button>
  );
}
