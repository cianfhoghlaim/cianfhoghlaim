/**
 * STUB for Convex generated API.
 *
 * Per the 2026-08-26 build subagent report: tuatha uses Convex but does
 * not have a generated `_generated/api` file (the canonical Convex CLI
 * output). The realm/$subject and student/$id/badges routes import it
 * for `api.<table>.<function>()` calls.
 */
export const api = {} as Record<string, Record<string, (...args: unknown[]) => Promise<unknown>>>;
