/**
 * Per-subject Convex types for LC Biology.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Biology* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { IdField, SystemFields } from "convex/server";

import type {
  BiologySyllabus,
  BiologyPaper,
  BiologyMarkingScheme,
  BiologyTopics,
} from "../../../../../baml_client/typescript/baml_client/lc/biology/client";

/**
 * The BAML-generated BiologyPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type BiologyPayload =
  | BiologySyllabus
  | BiologyPaper
  | BiologyMarkingScheme
  | BiologyTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface BiologyDoc extends IdField<"biology">, SystemFields {
  app: "oideachais";
  stage: "lc";
  subject: "biology";
  language: "en" | "ga";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: BiologyPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML biologyType alias for the schema file
export type biologyType = BiologyPayload;
