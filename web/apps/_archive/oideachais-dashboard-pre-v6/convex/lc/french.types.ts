/**
 * Per-subject Convex types for LC French.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated French* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { IdField, SystemFields } from "convex/server";

import type {
  FrenchSyllabus,
  FrenchPaper,
  FrenchMarkingScheme,
  FrenchTopics,
} from "../../../../../baml_client/typescript/baml_client/lc/french/client";

/**
 * The BAML-generated FrenchPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type FrenchPayload =
  | FrenchSyllabus
  | FrenchPaper
  | FrenchMarkingScheme
  | FrenchTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface FrenchDoc extends IdField<"french">, SystemFields {
  app: "oideachais";
  stage: "lc";
  subject: "french";
  language: "en" | "ga";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: FrenchPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML frenchType alias for the schema file
export type frenchType = FrenchPayload;
