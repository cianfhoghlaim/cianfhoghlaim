/**
 * STUB for `components/leaving-cert/BiologyPage` (and 7 sibling
 * per-subject page exports).
 *
 * Per the 2026-08-26 build subagent report: the BIEP per-subject page
 * renderer was planned for 8 subjects but never landed. The 8 route
 * files (lc/biology, lc/chemistry, lc/french, lc/irish, lc/mathematics,
 * lc/business, lc/history, lc/construction-studies) all import a
 * different named export from this single stub file. Replace with the
 * canonical per-subject page renderer once it lands.
 */
export function BiologyPage() { return <div>BiologyPage (stub)</div>; }
export function ChemistryPage() { return <div>ChemistryPage (stub)</div>; }
export function FrenchPage() { return <div>FrenchPage (stub)</div>; }
export function IrishPage() { return <div>IrishPage (stub)</div>; }
export function MathematicsPage() { return <div>MathematicsPage (stub)</div>; }
export function BusinessPage() { return <div>BusinessPage (stub)</div>; }
export function HistoryPage() { return <div>HistoryPage (stub)</div>; }
export function ConstructionStudiesPage() { return <div>ConstructionStudiesPage (stub)</div>; }
