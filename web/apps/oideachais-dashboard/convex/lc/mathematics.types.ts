/**
 * Per-subject Convex types for LC Mathematics.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Mathematics* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { Doc as BaseDoc } from "convex/server";

import type {
  MathematicsSyllabus,
  MathematicsPaper,
  MathematicsMarkingScheme,
  MathematicsTopics,
} from "../../../../baml_client/typescript/baml_client/lc/mathematics/client";

/**
 * The BAML-generated MathematicsPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type MathematicsPayload =
  | MathematicsSyllabus
  | MathematicsPaper
  | MathematicsMarkingScheme
  | MathematicsTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface MathematicsDoc extends BaseDoc {
  app: "oideachais";
  stage: "lc";
  subject: "mathematics";
  language: "en" | "ga";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: MathematicsPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML mathematicsType alias for the schema file
export type mathematicsType = MathematicsPayload;
