/**
 * Per-subject Convex types for GCSE Geography.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Geography* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { Doc as BaseDoc } from "convex/server";

import type {
  GeographySyllabus,
  GeographyPaper,
  GeographyMarkingScheme,
  GeographyTopics,
} from "../../../../baml_client/typescript/baml_client/gcse/geography/client";

/**
 * The BAML-generated GeographyPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type GeographyPayload =
  | GeographySyllabus
  | GeographyPaper
  | GeographyMarkingScheme
  | GeographyTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface GeographyDoc extends BaseDoc {
  app: "oideachais";
  stage: "gcse";
  subject: "geography";
  language: "en";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: GeographyPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML geographyType alias for the schema file
export type geographyType = GeographyPayload;
