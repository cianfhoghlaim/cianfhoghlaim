/**
 * Per-subject Convex types for A-LEVEL Sociology.
 *
 * AUTO-GENERATED by scripts/schema-codegen/convex-from-zod.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Re-exports the BAML-generated Sociology* types from step 1 + adds the
 * Convex-specific doc type.
 */

import type { Doc as BaseDoc } from "convex/server";

import type {
  SociologySyllabus,
  SociologyPaper,
  SociologyMarkingScheme,
  SociologyTopics,
} from "../../../../baml_client/typescript/baml_client/a-level/sociology/client";

/**
 * The BAML-generated SociologyPayload type (the union of the 4 extraction
 * functions' outputs).
 */
export type SociologyPayload =
  | SociologySyllabus
  | SociologyPaper
  | SociologyMarkingScheme
  | SociologyTopics;

/**
 * The per-subject Convex doc type (the BAML payload + Convex metadata).
 */
export interface SociologyDoc extends BaseDoc {
  app: "oideachais";
  stage: "a-level";
  subject: "sociology";
  language: "en";
  syllabus_topic_code: string;
  syllabus_topic_title: string;
  learning_outcome_code: string;
  learning_outcome_text: string;
  blooms_level:
    | "remember"
    | "understand"
    | "apply"
    | "analyse"
    | "evaluate"
    | "create";
  payload: SociologyPayload;
  ncca_code: string;
  created_at: number;
  updated_at: number;
}

// Re-export the BAML sociologyType alias for the schema file
export type sociologyType = SociologyPayload;
