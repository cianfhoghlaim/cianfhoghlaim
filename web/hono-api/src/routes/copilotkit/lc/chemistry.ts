/**
 * CopilotKit actions for LC Chemistry.
 *
 * AUTO-GENERATED by scripts/schema-codegen/copilotkit-actions.ts (Phase O).
 * DO NOT EDIT — re-run `bun run scripts/schema-codegen/index.ts` to update.
 *
 * Mounted at /api/copilotkit/lc/chemistry/* in web/hono-api/src/index.ts.
 *
 * Per openspec/changes/2026-08-13-web-monorepo-consolidation-and-agent-integration-v1/
 * specs/per-subject-coverage/spec.md.
 */

import { Hono } from "hono";

import { studyPlanStubResponse } from "./_study_plan_stub";

const chemistryApp = new Hono()
  .get("/health", (c) =>
    c.json({
      status: "ok",
      subject: "chemistry",
      stage: "lc",
      display_name: "Chemistry",
      actions: 13,
    }),
  )
  .post("/get_syllabus_topics", (c) => c.json({ stub: true }))
  .post("/get_study_plan", async (c) => {
    // Per the 2026-09-01-cianfhoghlaim-nua-end-to-end-showcase-v1 change
    // (Phase 1, §3.2 of tasks.md). Phase 1 ships the canonical stub
    // response shape (matches the Python planner at
    // `agents/adk/subjects/lc/planner.py::generate_study_plan(...)`).
    // Phase 5+ replaces this with a real HTTP bridge to the deployed
    // Python planner service.
    const body = (await c.req.json().catch(() => ({}))) as Record<string, unknown>;
    return c.json(
      studyPlanStubResponse("chemistry", {
        user_id: typeof body.user_id === "string" ? body.user_id : undefined,
        trace_id: typeof body.trace_id === "string" ? body.trace_id : undefined,
        lo_codes: Array.isArray(body.lo_codes) ? (body.lo_codes as string[]) : undefined,
        target_date:
          typeof body.target_date === "string" ? body.target_date : undefined,
        duration_weeks:
          typeof body.duration_weeks === "number" ? body.duration_weeks : 12,
        language: typeof body.language === "string" ? body.language : "en",
      }),
    );
  })
  // (13 actions per the canonical CopilotKit action surface)
  ;

// Stub handlers for the 13 actions
const handlers = {
  get_syllabus_topics: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_syllabus_topics()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_exam_papers: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_exam_papers()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_marking_schemes: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_marking_schemes()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_topic_detail: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_topic_detail()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_cross_jurisdictional_equivalences: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_cross_jurisdictional_equivalences()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  semantic_search: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:semantic_search()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  extract_syllabus_from_pdf: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:extract_syllabus_from_pdf()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  save_annotation: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:save_annotation()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  track_progress: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:track_progress()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_study_plan: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_study_plan()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  compare_curricula: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:compare_curricula()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  get_glossary_term: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:get_glossary_term()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  },
  extract_learning_outcome: async (params: unknown) => {
    // Forward to agents/adk/subjects/lc/chemistry_agent.py:extract_learning_outcome()
    // In production, this calls the Python per-subject agent.
    return JSON.stringify({ stub: true, subject: slug, stage: stage, action: a.name, params });
  }
};

export default chemistryApp;
