version: "3.8"

services:
  postgres:
    image: postgres:latest
    container_name: tanstack_betterauth
    restart: always
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - 5432:5432
    volumes:
      - tanstack_betterauth_data:/var/lib/postgresql/data
    networks:
      - default

networks:
  default:
    name: tanstack_betterauth

volumes:
  tanstack_betterauth_data:
