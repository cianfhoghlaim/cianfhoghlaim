# Dagster dg workspace configuration
# Loads all data pipeline projects in a unified UI.
# Run `dg dev` from repository root to start all code locations.

directory_type = "workspace"

[workspace]

# Celtic Education Platform — Irish curriculum, Celtic languages, British Isles education
[[workspace.locations]]
path = "sruth/oideachais"

# AI/ML quadrant — 12 agents, OCR/HTR, Celtic-language DLT
[[workspace.locations]]
path = "sruth/meaisinfhoghlaim"

# Celtic Educational MMO + Crypto Platform (Babylon.js + SpacetimeDB + crypteolas MMO-crypto)
[[workspace.locations]]
path = "sruth/tuatha"

# Crypteolas — crypto data intelligence (the standalone DeFi/crypto platform)
[[workspace.locations]]
path = "sruth/crypteolas"

# Crypteolas Demo — the standalone TanStack + Agno demo app
[[workspace.locations]]
path = "sruth/crypteolas/apps/crypteolas_demo"

# Croílár — multi-persona portfolio + CV + data engineering (Stream-registry DLT)
[[workspace.locations]]
path = "sruth/croilar"
