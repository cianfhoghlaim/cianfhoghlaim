# Thin re-export shim for the meaisinfoghlaim secrets.env (round 7).
# Canonical location: infrastructure/stacks/meaisinfoghlaim/secrets.env
# The actual infisical://dev-baile/<svc>/<key> URIs are read from
# the canonical file at runtime.
---
# See compose.yaml in this directory for the re-export note.
