[package]
name = "tuath-game"
description = "SpacetimeDB module for Tuath Celtic MMO game state"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { workspace = true }
cianfhoghlaim-shared-types = { path = "../shared-types", features = ["spacetimedb"] }

[dev-dependencies]
# SpacetimeDB test utilities
