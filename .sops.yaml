---
# SOPS configuration — Cianfhoghlaim / Kings' College Galway
# Age public key stored here (committed). Private key stored in Infisical
# at dev-baile/sops/age_key. Runtime decryption via Locket sidecar.
# Per AGENTS.md: never commit plaintext secrets. .env is gitignored;
# .env.enc is SOPS-encrypted and commit-safe.

creation_rules:
  # Environment files
  - path_regex: \.env$
    encrypted_regex: ^(.*)$
    age: age1ucuy60fykra7p49x8qx9lha9qsv444fy4vkckupv8nupat0jg5nscdgnkr

  # Locket secrets templates
  - path_regex: secrets\.env$
    encrypted_regex: ^(.*)$
    age: age1ucuy60fykra7p49x8qx9lha9qsv444fy4vkckupv8nupat0jg5nscdgnkr

  # Infisical template
  - path_regex: \.infisical\.env$
    encrypted_regex: ^(.*)$
    age: age1ucuy60fykra7p49x8qx9lha9qsv444fy4vkckupv8nupat0jg5nscdgnkr

  # Ansible vault
  - path_regex: vault\.yml$
    encrypted_regex: ^(.*)$
    age: age1ucuy60fykra7p49x8qx9lha9qsv444fy4vkckupv8nupat0jg5nscdgnkr

minimum_version: "3.13.0"
