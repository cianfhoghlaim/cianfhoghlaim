{
  "name": "croilar-mcp-devtools",
  "version": "0.1.0",
  "description": "MCP server exposing the croilar-devtools-hub web stack telemetry to Claude (and other MCP clients).",
  "type": "module",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "bun run index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
