# Croílár — Multi-persona portfolio + CV + data engineering
# Phase 0.1 of lateralise-british-isles-domains. Registers the
# Stream-registry-driven DLT + CocoIndex Dagster assets so the
# Croílár code-location shows up in `dg dev` from the repo root.

directory_type = "project"

[project]
name = "croilar"

[project.python_environment]
uv_managed = true

[project.code_location]
module_name = "definitions"
location_name = "croilar"
