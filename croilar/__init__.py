"""croílár — multi-persona portfolio platform.

A sub-package of the `cianfhoghlaim` monorepo. Houses the
personal portfolio + CV + data-engineering pipelines that
the operator (Cian Mac an Déisigh Uí Liatháin) uses as a
reference implementation of the wider monorepo's patterns
(DLT + Dagster + LanceDB + Convex + Hono + TanStack Start).

The package is consumed by 5 user-named stacks under
`infrastructure/stacks/croilar-{convex, dagster,
hono-api, marimo, web}/`.

See `croilar/README.md` for the canonical documentation.
"""
