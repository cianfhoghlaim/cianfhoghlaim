# orchestration/pipelines/education/tertiary/uog/students_union/defs.yaml
#
# L3 PipelineFactoryComponent for the University of Galway students'
# union pipeline (events + society pages + club content).
#
# Pipeline kind: official_docs (re-used for HTML scraping + schema
# inference). The legacy flat file was `uog_students_union.py`.
#
# Reference: openspec/changes/2026-08-24-wave-2-orchestration-vertical-pipelines-v1/
type: orchestration.components.PipelineFactoryComponent
attributes:
  dlt_source: dlt_sources.education.tertiary.uog.students_union
  pipeline_kind: official_docs
  embedding_model: BAAI/bge-large-en-v1.5
  hnsw_index: true
  conformance_required: true
  destinations:
    - ducklake_cianfhoghlaim
  processing:
    - cocoindex_live_update
  schedules:
    - cron: "0 12 * * *"
      timezone: UTC
  sensors:
    - upstream_change
  tags:
    - tertiary
    - uog
    - students_union
    - events
    - societies
  metadata:
    openspec_change: 2026-08-24-wave-2-orchestration-vertical-pipelines-v1
    spec: orchestration-vertical-pipelines
    kind: official_docs
    institution: university_of_galway
    legacy_files:
      - orchestration/defs/uog_students_union.py
