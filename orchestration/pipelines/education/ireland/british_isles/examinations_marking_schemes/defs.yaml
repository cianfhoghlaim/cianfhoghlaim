# orchestration/pipelines/education/ireland/british_isles/examinations_marking_schemes/defs.yaml
#
# Per-pipeline Component (Wave 2 of the 2026-08-24 master refactor).
#
# Mirrors the `dlt_sources.education.ireland.british_isles.examinations_marking_schemes` source. Generated by
# `scripts/sync/scaffold_pipelines_tree.py`. Per the canonical
# `dagster-pipeline-components` spec, this Component uses the
# canonical `dagster_dlt.DltLoadCollectionComponent` + the Cianfhoghlaim
# `translation:` defaults from
# `orchestration.pipelines._shared.dagster_dlt_integration`.
#
# Pipeline kind: pipeline
# Group name: 1_ingestion_education_cross_pipeline
type: dagster_dlt.DltLoadCollectionComponent
attributes:
  loads:
    - pipeline: orchestration.pipelines._shared.dagster_dlt_integration:build_dlt_pipeline|dlt_sources.education.ireland.british_isles.examinations_marking_schemes
      source: dlt_sources.education.ireland.british_isles.examinations_marking_schemes
      translation: orchestration.pipelines._shared.dagster_dlt_integration.kcg_default_translation
