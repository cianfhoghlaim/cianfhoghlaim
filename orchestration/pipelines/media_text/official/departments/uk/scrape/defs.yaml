# orchestration/pipelines/media_text/official/departments/uk/scrape/defs.yaml
#
# Per-pipeline Component (Wave 2 of the 2026-08-24 master refactor).
#
# Mirrors the `dlt_sources.media_text.official.departments.uk.scrape` source. Generated by
# `scripts/sync/scaffold_pipelines_tree.py`. Per the canonical
# `dagster-pipeline-components` spec, this Component uses the
# canonical `dagster_dlt.DltLoadCollectionComponent` + the Cianfhoghlaim
# `translation:` defaults from
# `orchestration.pipelines._shared.dagster_dlt_integration`.
#
# Pipeline kind: pipeline
# Group name: 1_ingestion_media_text_cross_pipeline
type: dagster_dlt.DltLoadCollectionComponent
attributes:
  loads:
    - pipeline: orchestration.pipelines._shared.dagster_dlt_integration:build_dlt_pipeline|dlt_sources.media_text.official.departments.uk.scrape
      source: dlt_sources.media_text.official.departments.uk.scrape
      translation: orchestration.pipelines._shared.dagster_dlt_integration.kcg_default_translation
