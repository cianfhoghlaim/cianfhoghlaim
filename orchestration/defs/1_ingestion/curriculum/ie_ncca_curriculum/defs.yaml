# defs/1_ingestion/curriculum/ie_ncca_curriculum/defs.yaml
#
# L1 CelticIngestionComponent for the NCCA curriculum source (IE).
# Per the cianfhoghlaim-pipeline spec, the 5 high-churn sources
# (NCCA/SEC/CCEA/SQA/WJEC) use the state-backed Component pattern
# with state_refresh_interval="monthly" (per user direction).
type: orchestration.components.CelticIngestionComponent
attributes:
  source_id: ie.education.ncca
  domain: curriculum
  nation: ie
  automation: on_cron
  automation_cron: "0 0 1 9 *"
  state_backed: true
  state_refresh_interval: monthly
