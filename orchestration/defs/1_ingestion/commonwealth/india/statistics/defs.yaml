# defs/1_ingestion/commonwealth/ind/statistics/defs.yaml
#
# L1 ingestion for the India statistics DLT source.
#
# Per the canonical cross-region path contract this source lives at
# ``dlt/commonwealth/ind/statistics/mospi.py`` with
# ``source_id="commonwealth.ind.statistics.mospi"`` and lands in
# the DuckLake table ``oideachais.statistics.commonwealth.ind``.
#
# Reference: openspec/changes/2026-07-11-commonwealth-pipeline-v1/
type: cianfhoghlaim.orchestration.components.CelticIngestionComponent
attributes:
  source_name: commonwealth_ind_statistics
  module: cianfhoghlaim.dlt.commonwealth.ind.statistics.mospi
  schedule:
    cron: "0 4 * * *"
    timezone: UTC
  partitions:
    - region:
        values: ["commonwealth"]
    - country:
        values: ["ind"]
    - language:
        values: ["en", "hi"]
  use_local_scrapes: true
  asset_check: commonwealth_ind_statistics_partition_min
  asset_check_min_rows: 1
  tags:
    - commonwealth
    - ind
    - statistics
    - ingestion
  metadata:
    openspec_change: 2026-07-11-commonwealth-pipeline-v1
    spec: commonwealth-pipeline
