# defs/1_ingestion/commonwealth/aus/medicine/defs.yaml
#
# L1 ingestion for the Australia medicine DLT source.
#
# Per the canonical cross-region path contract this source lives at
# ``dlt/commonwealth/aus/medicine/tga.py`` with
# ``source_id="commonwealth.aus.medicine.tga"`` and lands in
# the DuckLake table ``oideachais.medicine.commonwealth.aus``.
#
# Reference: openspec/changes/2026-07-11-commonwealth-pipeline-v1/
type: cianfhoghlaim.orchestration.components.CelticIngestionComponent
attributes:
  source_name: commonwealth_aus_medicine
  module: cianfhoghlaim.dlt.commonwealth.aus.medicine.tga
  schedule:
    cron: "0 4 * * *"
    timezone: UTC
  partitions:
    - region:
        values: ["commonwealth"]
    - country:
        values: ["aus"]
    - language:
        values: ["en"]
  use_local_scrapes: true
  asset_check: commonwealth_aus_medicine_partition_min
  asset_check_min_rows: 1
  tags:
    - commonwealth
    - aus
    - medicine
    - ingestion
  metadata:
    openspec_change: 2026-07-11-commonwealth-pipeline-v1
    spec: commonwealth-pipeline
