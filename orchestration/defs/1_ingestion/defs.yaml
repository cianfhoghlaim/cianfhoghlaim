# defs/1_ingestion/defs.yaml — Layer 1 Ingestion root mount.
#
# Per the 2026-06-30-dagster-ground-up-rewrite-5-layer-component-architecture
# change, Layer 1 wraps one DLT source per @asset via
# `CelticIngestionComponent`. The 5 layer sub-folders below each hold
# one or more Component YAML defs files.
#
# Per openspec/specs/dagster-5-layer-component-architecture/spec.md
# (Requirement: 5-Layer `defs/` Tree) + openspec/specs/cianfhoghlaim-pipeline/spec.md
# (Requirement: 5-Layer Component Architecture (L1 Ingestion)).
---
type: dagster.DefsFolderComponent
attributes: {}
