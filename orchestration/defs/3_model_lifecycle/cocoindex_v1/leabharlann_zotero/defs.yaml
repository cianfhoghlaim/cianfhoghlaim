# defs/3_model_lifecycle/cocoindex_v1/leabharlann_zotero/defs.yaml
#
# L3 CelticModelLifecycleComponent for the LeabharlannZoteroEmbedding v1 App.
# Per the oideachais-cocoindex-v1 skill, the R1–R4 conformance contract
# is enforced at build_defs time by the Component. is_virtual=True so
# the LanceDB table mirrors its L1 upstream automatically.
type: cianfhoghlaim.orchestration.components.CelticModelLifecycleComponent
attributes:
  app_name: LeabharlannZoteroEmbedding
  module: cianfhoghlaim.cocoindex.leabharlann_embedding
  embedding_model: BAAI/bge-large-en-v1.5
  hnsw_index: true
  conformance_required: true
