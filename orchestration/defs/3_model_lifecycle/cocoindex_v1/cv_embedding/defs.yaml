# defs/3_model_lifecycle/cocoindex_v1/cv_embedding/defs.yaml
#
# L3 CelticModelLifecycleComponent for the CVEmbedding v1 App.
# Per the croilar-cv-extraction spec — `Author CV BAML + CocoIndex v1`
# Requirement. Embeds the author's CV / achievements / teaching PDFs into
# LanceDB for semantic search.
type: orchestration.components.CelticModelLifecycleComponent
attributes:
  app_name: CVEmbedding
  module: cianfhoghlaim.cocoindex_flows.cv.embedding
  embedding_model: BAAI/bge-large-en-v1.5
  hnsw_index: true
  conformance_required: true