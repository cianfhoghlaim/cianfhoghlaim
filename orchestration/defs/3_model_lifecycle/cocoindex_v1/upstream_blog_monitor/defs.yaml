# defs/3_model_lifecycle/cocoindex_v1/upstream_blog_monitor/defs.yaml
#
# L3 CelticModelLifecycleComponent for the UpstreamBlogMonitor v1 App.
# Per the cianfhoghlaim-cocoindex-v1 skill, the R1–R4 conformance contract
# is enforced at build_defs time by the Component. is_virtual=True so
# the LanceDB table mirrors its L1 upstream automatically.
type: orchestration.components.CelticModelLifecycleComponent
attributes:
  app_name: UpstreamBlogMonitor
  module: cianfhoghlaim.cocoindex_flows.infrastructure.upstream_blog_monitor
  embedding_model: BAAI/bge-large-en-v1.5
  hnsw_index: true
  conformance_required: true
