# =============================================================================
# OIDEACHAIS - Celtic Education Platform
# =============================================================================
# Pan-Celtic curriculum search, AG-UI streaming, and learning outcomes API.
#
# USAGE:
#   Development:  docker compose -f compose.yaml -f compose.dev.yaml up -d
#   Production:   locket exec --env-file secrets.env -- docker compose up -d
#   Standalone:   docker compose up -d  (uses defaults)
# =============================================================================

name: oideachais

services:
  # ---------------------------------------------------------------------------
  # API - FastAPI Backend with AG-UI Streaming
  # ---------------------------------------------------------------------------
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oideachais-api
    restart: unless-stopped
    ports:
      - "${API_PORT:-8000}:8000"
    environment:
      # Application
      HOST: "0.0.0.0"
      PORT: "8000"
      ENVIRONMENT: ${ENVIRONMENT:-production}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      # Database paths (Unified on bunchloch NVMe)
      DUCKDB_PATH: /app/storage/data/celtic_education.duckdb
      LANCEDB_PATH: /app/storage/data/curriculum_vectors.lance
      # LLM Configuration (Routed through LiteLLM Gateway)
      LLM_API_KEY: ${LITELLM_MASTER_KEY:-sk-1234}
      LLM_PROVIDER: ${LLM_PROVIDER:-litellm}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://litellm:4000}
      LLM_MODEL: ${LLM_MODEL:-gemini/gemini-2.0-flash} # Updated to latest Gemini
      # Observability
      LANGFUSE_PUBLIC_KEY: ${LANGFUSE_PUBLIC_KEY:-}
      LANGFUSE_SECRET_KEY: ${LANGFUSE_SECRET_KEY:-}
      LANGFUSE_HOST: ${LANGFUSE_HOST:-http://langfuse:3000}
    volumes:
      - oideachais-data:/app/storage/data
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 16G # Leverages 48GB MacBook RAM
    networks:
      - oideachais

  # ---------------------------------------------------------------------------
  # FRONTEND - TanStack Start Web Application
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: oideachais-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    environment:
      NODE_ENV: ${ENVIRONMENT:-production}
      VITE_API_URL: ${API_URL:-http://api:8000}
      VITE_API_PUBLIC_URL: ${API_PUBLIC_URL:-https://api.oideachais.cianfhoghlaim.ie}
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3000"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    depends_on:
      api:
        condition: service_healthy
    networks:
      - oideachais

  # ---------------------------------------------------------------------------
  # AI GATEWAY - LiteLLM (Pangolin Converged)
  # ---------------------------------------------------------------------------
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: oideachais-litellm
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY:-sk-1234}
      VERTEX_PROJECT: ${VERTEX_PROJECT}
      VERTEX_LOCATION: ${VERTEX_LOCATION:-us-central1}
      GOOGLE_APPLICATION_CREDENTIALS: /app/key.json
    volumes:
      - ./sruth/bonneagar/templates/litellm/config.yaml:/app/config.yaml:ro
      - ${GCP_CREDENTIALS_PATH:-./secrets/gcp-key.json}:/app/key.json:ro
    command: [ "--config", "/app/config.yaml", "--port", "4000" ]
    networks:
      - oideachais

  # ---------------------------------------------------------------------------
  # RESEARCH - Perplexica / Vane (Converged Search)
  # ---------------------------------------------------------------------------
  perplexica:
    image: itisshams/perplexica:latest
    container_name: oideachais-research
    restart: unless-stopped
    environment:
      OPENAI_API_KEY: ${LITELLM_MASTER_KEY}
      OPENAI_API_BASE: http://litellm:4000/v1
    networks:
      - oideachais
    deploy:
      resources:
        limits:
          memory: 4G

# =============================================================================
# VOLUMES
# =============================================================================
volumes:
  oideachais-data:
    driver: local

# =============================================================================
# NETWORKS
# =============================================================================
networks:
  oideachais:
    driver: bridge
